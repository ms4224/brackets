import { MatchConnectionDrawDirective } from './match-connection-draw.directive';

xdescribe('MatchConnectionDrawDirective', () => {
  it('should create an instance', () => {
    // const directive = new MatchConnectionDrawDirective();
    // expect(directive).toBeTruthy();
  });
});
